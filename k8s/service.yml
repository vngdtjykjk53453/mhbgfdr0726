apiVersion: v1
kind: Service
metadata:
  name: rtehbhy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rtehbhy"
    port: 8080
  selector:
    app: rtehbhy
